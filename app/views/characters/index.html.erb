<h1>Liste des personnes</h1>

<table class="table table-striped">
  <thead>
    <tr class="info">
      <th>Nom</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @characters.each do |character| %>
      <tr>
        <td><%= character.first_name %></td>
        <td><%= link_to 'Voir', character %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= paginate @characters %>
